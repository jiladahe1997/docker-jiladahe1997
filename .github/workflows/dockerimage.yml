name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: cd nginx-proxy && docker build . --file Dockerfile --tag jiladahe1997/nginx-proxy:latest && docker push jiladahe1997/nginx-proxy:latest
